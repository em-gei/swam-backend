management:
  endpoints:
    web:
      base-path: /manage
      exposure:
        include: health,info,metrics
endpoint:
  health:
    show-details: always
server:
  port: 8090
  servlet:
    context-path: /bikeworld
